---
# Where to persist on the AWX host. Ensure this path is exposed in AWX Settings → Jobs.
artifact:
  to_path: true
  path: "/srv/awx-artifacts/reports"
  to_stdout: false
  to_email: false

smtp:
  host: smtp.office365.com
  port: 587
  starttls: true
  from: automation@yourdomain.com
  to:
    - you@yourdomain.com

report_title: "Windows Security Compliance Report"